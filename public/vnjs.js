let index = 0; let blank = ""; const speech = [ "Woah, it's been so long since we've seen a new student!!!", "I'm Misa! I'm the best student in this entire school! What's your name?", "Hi there!!! Nice to meet you!", "Anyways, wanna be friends?", "She doesn't wait for your response.", "Really? Yay! Let's go eat lunch together later!", "Oh... looks like the teacher's already here... We gotta get back to our seats!!!", "Are you gonna pay attention?", "In that case, I'll pay attention, too!!!!!", "In that case, I won't listen either!!!", "To your seats, students! Let's begin the lesson.", "Today, we have a new student!", "Why don't you introduce yourself?" ]; const names = [ "Misa Amane", "Narrator", "Teacher", "You" ]; const nameBox = document.getElementById("name"); const textBox = document.getElementById("thingy"); const btn = document.getElementById("next"); nameBox.textContent = names[0]; textBox.textContent = speech[0]; btn.addEventListener("click", () => { index++; if (index === 2) { playerName = prompt("what's your name? :3") || "You"; nameBox.textContent = names[0]; textBox.textContent = speech[index]; return; } if (index === 4){ nameBox.textContent= names[1]; } if (index === 5){ nameBox.textContent = names[0]; } if (index === 11) { nameBox.textContent = names[2]; } if (index === 8) { const answer = prompt("Y / N (if your answer is Y, type down Y only, otherwise, any other answers will be considered as a NO!)"); nameBox.textContent = names[0]; if (answer === "Y") { textBox.textContent = speech[8]; index = 11; } else { textBox.textContent = speech[9]; index = 11; } index++; return; } if (index >= speech.length) return; textBox.textContent = speech[index];});